from servicenow import snow
from archer import archer

arch = archer()
snow = snow()

#snowIssueDataArray = snow.getRecords(table='sn_grc_issue',query='',fields='number,sys_id,short_description,description')

#for issue in snowIssueDataArray:
    #print(issue)